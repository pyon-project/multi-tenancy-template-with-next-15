# Domain Configuration
NEXT_PUBLIC_ROOT_DOMAIN=localhost
NEXT_PUBLIC_VERCEL_DEPLOYMENT_SUFFIX=vercel.app

# App Names (used for routing and build configuration)
NEXT_PUBLIC_APP_NAME=all

# Development Ports (used for local development)
STORE_PORT=3001
BOARD_PORT=3002
TRADE_PORT=3003

# Example API URLs (replace with actual endpoints in production)
API_BASE_URL=http://localhost:4000

# Example Store Variables
STORE_CURRENCY=USD
STORE_TEST="This is a test environment variable for Store"

# Example Board Variables
BOARD_MAX_USERS_PER_BOARD=50
BOARD_STORAGE_PROVIDER=local

# Example Trade Variables
TRADE_DEFAULT_CURRENCY=USD
TRADE_MARKET_DATA_PROVIDER=mock
